<h2>Edit area</h2>

<%= simple_form_for @area, method: :put, validate: true do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name, autofocus: true %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, 'Save' %>
  </div>
<% end %>
