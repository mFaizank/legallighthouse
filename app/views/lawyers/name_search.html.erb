<h2>Search</h2>

<%= simple_form_for(@lawyer, url: lawyers_path, method: :get) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :search_name, label: 'Name' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit, "Search" %>
  </div>
<% end %>
